@using System.Globalization
@using System.Text.Json
@model EchoConsole.Web.Models.SessionEventAnalytics.SessionEventAnalyticsIndexViewModel

@{
    ViewData["Title"] = SharedLocalizer["Analytics_PageTitle"].Value;
    ViewData["TitleResourceKey"] = "Analytics_PageTitle";

    var chartPayload = new
    {
        timeSeries = new
        {
            labels = Model.TimeSeries?.Select(x => x.Label) ?? Array.Empty<string>(),
            values = Model.TimeSeries?.Select(x => x.Count) ?? Array.Empty<int>()
        },
        eventTypes = new
        {
            labels = Model.EventTypes?.Select(x => x.Label) ?? Array.Empty<string>(),
            values = Model.EventTypes?.Select(x => x.Count) ?? Array.Empty<int>()
        },
        scenes = new
        {
            labels = Model.Scenes?.Take(12).Select(x => x.Label) ?? Array.Empty<string>(),
            values = Model.Scenes?.Take(12).Select(x => x.Count) ?? Array.Empty<int>()
        },
        buildVersions = new
        {
            labels = Model.BuildVersions?.Select(x => x.Label) ?? Array.Empty<string>(),
            values = Model.BuildVersions?.Select(x => x.Count) ?? Array.Empty<int>()
        }
    };

    var chartJson = JsonSerializer.Serialize(
        chartPayload,
        new JsonSerializerOptions
        {
            PropertyNamingPolicy = JsonNamingPolicy.CamelCase
        });

    var chartLabels = new
    {
        events = SharedLocalizer["Analytics_ChartEvents"].Value,
        eventTypes = SharedLocalizer["Analytics_ChartEventTypes"].Value
    };

    var chartLabelsJson = JsonSerializer.Serialize(chartLabels);
}

<div class="min-h-screen bg-[#070B12] text-slate-100">
    <div class="mx-auto max-w-[1700px] px-6 py-8">
        <section class="rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-8 shadow-[0_0_30px_rgba(0,255,255,0.05)]">
            <div class="flex flex-col gap-4 xl:flex-row xl:items-start xl:justify-between">
                <div>
                    <p class="font-display text-xs uppercase tracking-[0.35em] text-cyan-300">
                        @SharedLocalizer["Analytics_Badge"]
                    </p>

                    <h1 class="mt-3 font-display text-3xl uppercase tracking-[0.16em] text-white">
                        @SharedLocalizer["Analytics_HeroTitle"]
                    </h1>

                    <p class="mt-3 max-w-3xl text-sm leading-6 text-slate-400">
                        @SharedLocalizer["Analytics_HeroSubtitle"]
                    </p>
                </div>

                <a asp-controller="SessionEvents"
                   asp-action="Index"
                   class="rounded-xl border border-fuchsia-400/20 bg-fuchsia-500/10 px-5 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-fuchsia-200 transition hover:bg-fuchsia-500/15">
                    @SharedLocalizer["Analytics_OpenRecentEvents"]
                </a>
            </div>
        </section>

        <section class="mt-6 rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-6">
            <form method="get"
                  asp-controller="SessionEventAnalytics"
                  asp-action="Index"
                  class="grid grid-cols-1 gap-4 lg:grid-cols-5">
                <div>
                    <label for="buildVersion"
                           class="mb-2 block text-xs font-semibold uppercase tracking-[0.20em] text-slate-400">
                        @SharedLocalizer["Common_Build"]
                    </label>

                    <select id="buildVersion"
                            name="buildVersion"
                            class="w-full rounded-xl border border-cyan-500/20 bg-slate-900/80 px-4 py-3 text-sm text-slate-100 outline-none focus:border-cyan-400/40">
                        <option value="">@SharedLocalizer["Common_AllBuilds"]</option>

                        @if (Model.AvailableBuildVersions != null)
                        {
                            foreach (var option in Model.AvailableBuildVersions)
                            {
                                if (string.Equals(
                                Model.BuildVersion,
                                option,
                                StringComparison.OrdinalIgnoreCase))
                                {
                                    <option value="@option" selected>@option</option>
                                }
                                else
                                {
                                    <option value="@option">@option</option>
                                }
                            }
                        }
                    </select>
                </div>

                <div>
                    <label for="fromDate"
                           class="mb-2 block text-xs font-semibold uppercase tracking-[0.20em] text-slate-400">
                        @SharedLocalizer["Common_FromDateUtc"]
                    </label>

                    <input id="fromDate"
                           name="fromDate"
                           type="date"
                           value="@Model.FromDate?.ToString("yyyy-MM-dd")"
                           class="w-full rounded-xl border border-cyan-500/20 bg-slate-900/80 px-4 py-3 text-sm text-slate-100 outline-none focus:border-cyan-400/40" />
                </div>

                <div>
                    <label for="toDate"
                           class="mb-2 block text-xs font-semibold uppercase tracking-[0.20em] text-slate-400">
                        @SharedLocalizer["Common_ToDateUtc"]
                    </label>

                    <input id="toDate"
                           name="toDate"
                           type="date"
                           value="@Model.ToDate?.ToString("yyyy-MM-dd")"
                           class="w-full rounded-xl border border-cyan-500/20 bg-slate-900/80 px-4 py-3 text-sm text-slate-100 outline-none focus:border-cyan-400/40" />
                </div>

                <div>
                    <label for="trendGranularity"
                           class="mb-2 block text-xs font-semibold uppercase tracking-[0.20em] text-slate-400">
                        @SharedLocalizer["Analytics_TrendResolution"]
                    </label>

                    <select id="trendGranularity"
                            name="trendGranularity"
                            class="w-full rounded-xl border border-cyan-500/20 bg-slate-900/80 px-4 py-3 text-sm text-slate-100 outline-none focus:border-cyan-400/40">
                        <option value="day"
                                selected="@(Model.TrendGranularity == "day")">
                            @SharedLocalizer["Analytics_Daily"]
                        </option>
                        <option value="hour"
                                selected="@(Model.TrendGranularity == "hour")">
                            @SharedLocalizer["Analytics_Hourly"]
                        </option>
                    </select>
                </div>

                <div class="flex items-end gap-3">
                    <button type="submit"
                            class="rounded-xl border border-cyan-400/25 bg-cyan-500/10 px-5 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-cyan-200 transition hover:bg-cyan-500/15">
                        @SharedLocalizer["Common_Apply"]
                    </button>

                    <a asp-controller="SessionEventAnalytics"
                       asp-action="Index"
                       class="rounded-xl border border-slate-700 bg-slate-900/70 px-5 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-slate-300 transition hover:text-white">
                        @SharedLocalizer["Common_Reset"]
                    </a>
                </div>
            </form>

            @if (!string.IsNullOrWhiteSpace(Model.ErrorMessage))
            {
                <div class="mt-5 rounded-xl border border-rose-500/25 bg-rose-500/10 px-4 py-3 text-sm text-rose-200">
                    @Model.ErrorMessage
                </div>
            }
        </section>

        <section class="mt-6 grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-4">
            <div class="rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["Analytics_TotalEvents"]
                </div>
                <div class="mt-3 font-display text-4xl text-cyan-300"
                     data-analytics-counter="@Model.TotalEvents">
                    0
                </div>
            </div>

            <div class="rounded-2xl border border-fuchsia-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["Analytics_EventTypes"]
                </div>
                <div class="mt-3 font-display text-4xl text-fuchsia-300"
                     data-analytics-counter="@Model.EventTypeCount">
                    0
                </div>
            </div>

            <div class="rounded-2xl border border-amber-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["Analytics_Scenes"]
                </div>
                <div class="mt-3 font-display text-4xl text-amber-300"
                     data-analytics-counter="@Model.SceneCount">
                    0
                </div>
            </div>

            <div class="rounded-2xl border border-emerald-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["Analytics_Builds"]
                </div>
                <div class="mt-3 font-display text-4xl text-emerald-300"
                     data-analytics-counter="@Model.BuildCount">
                    0
                </div>
            </div>
        </section>

        @if (Model.TotalEvents == 0)
        {
            <section class="mt-6 rounded-2xl border border-slate-800 bg-slate-950/70 p-12 text-center">
                <p class="font-display text-sm uppercase tracking-[0.28em] text-slate-400">
                    @SharedLocalizer["Analytics_NoData"]
                </p>

                <p class="mx-auto mt-3 max-w-xl text-sm leading-6 text-slate-500">
                    @SharedLocalizer["Analytics_NoDataDescription"]
                </p>
            </section>
        }
        else
        {
            <script id="session-event-analytics-chart-data"
                    type="application/json">
                @Html.Raw(chartJson)
            </script>

            <section class="mt-6 rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-6 shadow-[0_0_40px_rgba(34,211,238,0.05)]">
                <div>
                    <p class="font-display text-xs uppercase tracking-[0.30em] text-cyan-300">
                        @SharedLocalizer["Analytics_ActivityTrend"]
                    </p>

                    <h2 class="mt-2 text-xl font-semibold text-white">
                        @SharedLocalizer["Analytics_EventsOverTime"]
                    </h2>
                </div>

                <div class="relative mt-6 h-[380px]">
                    <canvas id="event-trend-chart"></canvas>
                </div>
            </section>

            <div class="mt-6 grid grid-cols-1 gap-6 xl:grid-cols-2">
                <section class="rounded-2xl border border-fuchsia-500/20 bg-slate-950/70 p-6">
                    <p class="font-display text-xs uppercase tracking-[0.30em] text-fuchsia-300">
                        @SharedLocalizer["Analytics_Distribution"]
                    </p>

                    <h2 class="mt-2 text-xl font-semibold text-white">
                        @SharedLocalizer["Analytics_EventsByType"]
                    </h2>

                    <div class="relative mt-6 h-[380px]">
                        <canvas id="event-type-chart"></canvas>
                    </div>
                </section>

                <section class="rounded-2xl border border-fuchsia-500/20 bg-slate-950/70 p-6">
                    <p class="font-display text-xs uppercase tracking-[0.30em] text-fuchsia-300">
                        @SharedLocalizer["Analytics_HotZones"]
                    </p>

                    <h2 class="mt-2 text-xl font-semibold text-white">
                        @SharedLocalizer["Analytics_EventsByScene"]
                    </h2>

                    <div class="relative mt-6 h-[380px]">
                        <canvas id="scene-chart"></canvas>
                    </div>
                </section>
            </div>

            <section class="mt-6 rounded-2xl border border-amber-500/20 bg-slate-950/70 p-6">
                <p class="font-display text-xs uppercase tracking-[0.30em] text-amber-300">
                    @SharedLocalizer["Analytics_ReleaseComparison"]
                </p>

                <h2 class="mt-2 text-xl font-semibold text-white">
                    @SharedLocalizer["Analytics_EventsByBuild"]
                </h2>

                <div class="relative mt-6 h-[360px]">
                    <canvas id="build-chart"></canvas>
                </div>
            </section>
        }
    </div>
</div>

@section Scripts {
    <script src="~/lib/chart.js/chart.umd.min.js"
            asp-append-version="true"></script>

    <script src="~/js/session-event-analytics-charts.js"
            asp-append-version="true"></script>

    <script>
        window.echoConsoleAnalyticsCharts.init({
            dataElementId: "session-event-analytics-chart-data",
            trendCanvasId: "event-trend-chart",
            eventTypeCanvasId: "event-type-chart",
            sceneCanvasId: "scene-chart",
            buildCanvasId: "build-chart",
            culture: @Html.Raw(JsonSerializer.Serialize(CultureInfo.CurrentUICulture.Name)),
            labels: @Html.Raw(chartLabelsJson)
        });
    </script>
}