@using System.Globalization
@using System.Text.Json
@model EchoConsole.Web.Models.LiveOperations.LiveOperationsIndexViewModel

@{
    ViewData["Title"] = SharedLocalizer["LiveOps_PageTitle"].Value;
    ViewData["TitleResourceKey"] = "LiveOps_PageTitle";

    string SpikeLabel(string state) => state switch
    {
        "Spike" => SharedLocalizer["LiveOps_SpikeStateSpike"].Value,
        "Elevated" => SharedLocalizer["LiveOps_SpikeStateElevated"].Value,
        "Normal" => SharedLocalizer["LiveOps_SpikeStateNormal"].Value,
        _ => SharedLocalizer["LiveOps_SpikeStateQuiet"].Value
    };

    var clientLabels = new
    {
        connectionUnavailable = SharedLocalizer["LiveOps_ConnectionUnavailable"].Value,
        connectionReconnecting = SharedLocalizer["LiveOps_ConnectionReconnecting"].Value,
        connectionConnected = SharedLocalizer["LiveOps_ConnectionConnected"].Value,
        connectionDisconnected = SharedLocalizer["LiveOps_ConnectionDisconnected"].Value,
        connectionConnecting = SharedLocalizer["LiveOps_ConnectionConnecting"].Value,
        stateActive = SharedLocalizer["LiveOps_StateActive"].Value,
        stateDegraded = SharedLocalizer["LiveOps_StateDegraded"].Value,
        stateInactive = SharedLocalizer["LiveOps_StateInactive"].Value,
        spikeQuiet = SharedLocalizer["LiveOps_SpikeStateQuiet"].Value,
        spikeNormal = SharedLocalizer["LiveOps_SpikeStateNormal"].Value,
        spikeElevated = SharedLocalizer["LiveOps_SpikeStateElevated"].Value,
        spikeSpike = SharedLocalizer["LiveOps_SpikeStateSpike"].Value,
        scene = SharedLocalizer["LiveOps_Scene"].Value,
        gameState = SharedLocalizer["LiveOps_GameState"].Value,
        update = SharedLocalizer["LiveOps_Update"].Value,
        noHeartbeat = SharedLocalizer["LiveOps_NoHeartbeat"].Value,
        current = SharedLocalizer["LiveOps_CurrentWindow"].Value,
        previous = SharedLocalizer["LiveOps_PreviousWindow"].Value,
        noInstallations = SharedLocalizer["LiveOps_NoInstallations"].Value,
        noInstallationsDescription = SharedLocalizer["LiveOps_NoInstallationsDescription"].Value
    };

    var clientLabelsJson = JsonSerializer.Serialize(clientLabels);
    var initialFleetJson = JsonSerializer.Serialize(Model.Snapshot.Installations);
}

<div class="min-h-screen bg-[#070B12] text-slate-100">
    <div class="mx-auto max-w-[1750px] px-4 py-6 sm:px-6 sm:py-8">
        <section class="relative overflow-hidden rounded-3xl border border-cyan-500/20 bg-slate-950/80 p-6 shadow-[0_0_50px_rgba(34,211,238,0.06)] sm:p-8">
            <div class="absolute -right-24 -top-24 h-64 w-64 rounded-full bg-cyan-500/10 blur-3xl"></div>
            <div class="absolute -bottom-24 left-1/3 h-64 w-64 rounded-full bg-fuchsia-500/10 blur-3xl"></div>

            <div class="relative flex flex-col gap-6 xl:flex-row xl:items-start xl:justify-between">
                <div>
                    <p class="font-display text-xs uppercase tracking-[0.38em] text-cyan-300">
                        @SharedLocalizer["LiveOps_Badge"]
                    </p>

                    <h1 class="mt-3 font-display text-3xl uppercase tracking-[0.16em] text-white md:text-4xl">
                        @SharedLocalizer["LiveOps_HeroTitle"]
                    </h1>

                    <p class="mt-3 max-w-3xl text-sm leading-6 text-slate-400">
                        @SharedLocalizer["LiveOps_HeroSubtitle"]
                    </p>
                </div>

                <div class="flex flex-wrap gap-3">
                    <div id="live-ops-connection-badge"
                         class="inline-flex items-center gap-3 rounded-2xl border border-slate-700 bg-slate-900/80 px-5 py-3">
                        <span id="live-ops-connection-dot"
                              class="h-2.5 w-2.5 rounded-full bg-slate-500"></span>

                        <div>
                            <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                                @SharedLocalizer["LiveOps_RealtimeLink"]
                            </div>

                            <div id="live-ops-connection-text"
                                 class="mt-1 text-sm font-semibold text-slate-300">
                                @SharedLocalizer["LiveOps_ConnectionConnecting"]
                            </div>
                        </div>
                    </div>

                    <div class="rounded-2xl border border-cyan-400/20 bg-cyan-500/10 px-5 py-3">
                        <div class="text-xs uppercase tracking-[0.22em] text-cyan-400">
                            @SharedLocalizer["LiveOps_ServerUtc"]
                        </div>

                        <div id="live-ops-server-time"
                             class="mt-1 font-display text-sm text-white">
                            @Model.Snapshot.ServerTimeUtc.ToString("yyyy-MM-dd HH:mm:ss")
                        </div>
                    </div>
                </div>
            </div>
        </section>

        @if (!Model.IsAvailable)
        {
            <section class="mt-6 rounded-2xl border border-rose-500/30 bg-rose-500/10 px-5 py-4 text-sm text-rose-200">
                Live operations data is temporarily unavailable. The command center will continue retrying automatically.
            </section>
        }

        <section class="mt-6 grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">
            <article class="rounded-2xl border border-emerald-500/20 bg-slate-950/70 p-6 shadow-[0_0_24px_rgba(52,211,153,0.04)]">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_ActiveInstallations"]
                </div>

                <div id="live-ops-active-installations"
                     class="mt-3 font-display text-4xl text-emerald-300">
                    @Model.Snapshot.ActiveInstallations
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_ActiveInstallationsDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-amber-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_DegradedInstallations"]
                </div>

                <div id="live-ops-degraded-installations"
                     class="mt-3 font-display text-4xl text-amber-300">
                    @Model.Snapshot.DegradedInstallations
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_DegradedInstallationsDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-slate-700 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_InactiveInstallations"]
                </div>

                <div id="live-ops-inactive-installations"
                     class="mt-3 font-display text-4xl text-slate-300">
                    @Model.Snapshot.InactiveInstallations
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_InactiveInstallationsDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_ActiveSessions"]
                </div>

                <div id="live-ops-active-sessions"
                     class="mt-3 font-display text-4xl text-cyan-300">
                    @Model.Snapshot.ActiveSessions
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_ActiveSessionsDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-fuchsia-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_EventsLast5Minutes"]
                </div>

                <div id="live-ops-events-5"
                     class="mt-3 font-display text-4xl text-fuchsia-300">
                    @Model.Snapshot.EventsLast5Minutes
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_EventsLast5MinutesDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-fuchsia-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_EventsLast15Minutes"]
                </div>

                <div id="live-ops-events-15"
                     class="mt-3 font-display text-4xl text-fuchsia-200">
                    @Model.Snapshot.EventsLast15Minutes
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_EventsLast15MinutesDescription"]
                </div>
            </article>

            <article class="rounded-2xl border border-rose-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_AlertRate"]
                </div>

                <div class="mt-3 flex items-end gap-2">
                    <span id="live-ops-alert-rate"
                          class="font-display text-4xl text-rose-300">
                        @Model.Snapshot.AlertRatePerMinute.ToString("0.00", CultureInfo.CurrentCulture)
                    </span>

                    <span class="pb-1 text-xs uppercase tracking-[0.16em] text-slate-600">
                        @SharedLocalizer["LiveOps_PerMinute"]
                    </span>
                </div>

                <div class="mt-2 text-xs text-slate-500">
                    <span id="live-ops-unresolved-alerts">
                        @Model.Snapshot.UnresolvedAlerts
                    </span>
                    @SharedLocalizer["LiveOps_UnresolvedAlerts"]
                </div>
            </article>

            <article id="live-ops-spike-card"
                     class="rounded-2xl border border-cyan-500/20 bg-slate-950/70 p-6">
                <div class="text-xs uppercase tracking-[0.22em] text-slate-500">
                    @SharedLocalizer["LiveOps_EventPeakIndicator"]
                </div>

                <div id="live-ops-event-spike"
                     class="mt-3 font-display text-3xl uppercase text-cyan-300">
                    @SpikeLabel(Model.Snapshot.EventSpikeState)
                </div>

                <div id="live-ops-event-spike-detail"
                     class="mt-2 text-xs text-slate-500">
                    @SharedLocalizer["LiveOps_EventPeakDescription"]
                </div>
            </article>
        </section>

        <section class="mt-6 rounded-3xl border border-cyan-500/20 bg-slate-950/70 p-5 shadow-[0_0_40px_rgba(34,211,238,0.04)] sm:p-6">
            <div class="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
                <div>
                    <p class="font-display text-xs uppercase tracking-[0.34em] text-cyan-300">
                        @SharedLocalizer["LiveOps_FleetMatrix"]
                    </p>

                    <h2 class="mt-2 text-2xl font-semibold text-white">
                        @SharedLocalizer["LiveOps_InstallationStatusGrid"]
                    </h2>
                </div>

                <div class="text-sm text-slate-500">
                    @SharedLocalizer["LiveOps_Showing"]
                    <span id="live-ops-visible-installations"
                          class="font-semibold text-cyan-300">
                        0
                    </span>
                    @SharedLocalizer["Common_Of"]
                    <span id="live-ops-total-installations"
                          class="font-semibold text-cyan-300">
                        @Model.Snapshot.TotalInstallations
                    </span>
                </div>
            </div>

            <div class="mt-6 rounded-2xl border border-slate-800 bg-black/30 p-3">
                <div class="flex flex-col gap-3 xl:flex-row xl:items-center xl:justify-between">
                    <label for="nodeSearch" class="relative flex flex-1">
                        <div class="pointer-events-none absolute inset-y-0 left-0 flex items-center pl-6" aria-hidden="true">
                            <span class="font-mono text-sm font-bold text-green-500">&gt;</span>
                        </div>

                        <input type="text"
                               id="nodeSearch"
                               autocomplete="off"
                               spellcheck="false"
                               placeholder="SEARCH NODE OR CURRENT SCENE..."
                               class="block w-full rounded-xl border border-slate-800 bg-black py-5 pl-14 pr-4 font-mono text-sm tracking-wider text-green-400 outline-none transition placeholder:text-slate-600 focus:border-green-500/60 focus:ring-2 focus:ring-green-500/10" />
                    </label>

                    <div class="flex min-w-0 gap-2 overflow-x-auto"
                         role="tablist"
                         aria-label="Fleet state segments">
                        <button type="button"
                                id="activeNodesTab"
                                role="tab"
                                aria-selected="true"
                                aria-controls="activeNodesPanel"
                                data-fleet-tab="active"
                                class="shrink-0 rounded-xl border border-emerald-400/50 bg-emerald-500/15 px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.16em] text-emerald-200 shadow-[0_0_18px_rgba(52,211,153,0.08)]">
                            [ ACTIVE NODES
                            <span id="activeNodesTabCount">0</span> ]
                        </button>

                        <button type="button"
                                id="inactiveFleetTab"
                                role="tab"
                                aria-selected="false"
                                aria-controls="inactiveFleetPanel"
                                data-fleet-tab="inactive"
                                class="shrink-0 rounded-xl border border-slate-700 bg-slate-900/70 px-4 py-3 font-mono text-[11px] font-semibold uppercase tracking-[0.16em] text-slate-500">
                            [ INACTIVE FLEET
                            <span id="inactiveFleetTabCount">0</span> ]
                        </button>
                    </div>
                </div>
            </div>

            <div id="activeNodesPanel"
                 role="tabpanel"
                 aria-labelledby="activeNodesTab">
                <div id="live-ops-active-empty-state"
                     class="mt-6 hidden rounded-2xl border border-slate-800 bg-slate-900/60 p-10 text-center">
                    <p class="font-display text-sm uppercase tracking-[0.26em] text-slate-400">
                        NO ACTIVE NODES MATCH THE CURRENT FILTER
                    </p>

                    <p class="mt-3 text-sm text-slate-500">
                        Waiting for a session start or a fresh heartbeat from the fleet.
                    </p>
                </div>

                <div id="live-ops-active-grid"
                     class="mt-6 grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4"
                     aria-live="polite"></div>
            </div>

            <div id="inactiveFleetPanel"
                 role="tabpanel"
                 aria-labelledby="inactiveFleetTab"
                 class="hidden">
                <details id="inactiveFleetDetails"
                         class="mt-6 overflow-hidden rounded-2xl border border-slate-800 bg-slate-950/70"
                         open>
                    <summary class="flex cursor-pointer list-none items-center justify-between gap-4 border-b border-slate-800 px-5 py-4 font-mono text-xs uppercase tracking-[0.18em] text-slate-400 transition hover:bg-slate-900/70 hover:text-slate-200">
                        <span>[ CONSOLIDATED OFFLINE / DEGRADED TERMINALS ]</span>
                        <span class="text-slate-600">COLLAPSE / EXPAND</span>
                    </summary>

                    <div class="overflow-x-auto">
                        <table class="min-w-full table-fixed text-left text-xs">
                            <thead class="bg-black/30 font-mono uppercase tracking-[0.14em] text-slate-600">
                                <tr>
                                    <th class="w-[27%] px-4 py-3 font-medium">Node</th>
                                    <th class="w-[14%] px-4 py-3 font-medium">State</th>
                                    <th class="w-[20%] px-4 py-3 font-medium">Last scene</th>
                                    <th class="w-[15%] px-4 py-3 font-medium">Build</th>
                                    <th class="w-[24%] px-4 py-3 font-medium">Last signal</th>
                                </tr>
                            </thead>

                            <tbody id="live-ops-inactive-table-body"
                                   class="divide-y divide-slate-800/80"></tbody>
                        </table>
                    </div>

                    <div id="live-ops-inactive-empty-state"
                         class="hidden px-5 py-10 text-center font-mono text-xs uppercase tracking-[0.16em] text-slate-600">
                        No inactive terminals match the current filter.
                    </div>

                    <div id="live-ops-inactive-pagination"
                         class="hidden border-t border-slate-800 px-5 py-4 text-center">
                        <button type="button"
                                id="loadMoreInactiveNodes"
                                class="rounded-lg border border-cyan-500/20 bg-cyan-500/5 px-4 py-2 font-mono text-[10px] uppercase tracking-[0.16em] text-cyan-400 transition hover:border-cyan-400/50 hover:bg-cyan-500/10">
                            [ LOAD MORE TERMINALS ]
                        </button>
                    </div>
                </details>
            </div>

            <div class="mt-5 flex flex-col gap-2 border-t border-slate-800/80 pt-4 text-xs text-slate-600 sm:flex-row sm:items-center sm:justify-between">
                <span>
                    Active cards remain detailed. Inactive nodes are rendered lazily in compact batches to protect the DOM.
                </span>

                <span>
                    @SharedLocalizer["LiveOps_LastRefreshed"]
                    <span id="live-ops-last-refresh">
                        @Model.Snapshot.ServerTimeUtc.ToString("HH:mm:ss 'UTC'")
                    </span>
                </span>
            </div>
        </section>
    </div>
</div>

@section Scripts {
    <script src="~/lib/signalr/signalr.min.js"
            asp-append-version="true"></script>

    <script src="~/js/live-operations.js"
            asp-append-version="true"></script>

    <script>
        window.echoConsoleLiveOperations.init({
            hubUrl: "/hubs/admin-telemetry",
            snapshotUrl: "/Admin/LiveOperations/Snapshot",
            refreshIntervalMilliseconds: 15000,
            inactivePageSize: 100,
            culture: @Html.Raw(JsonSerializer.Serialize(CultureInfo.CurrentUICulture.Name)),
            labels: @Html.Raw(clientLabelsJson),
            initialFleet: @Html.Raw(initialFleetJson)
        });
    </script>
}
